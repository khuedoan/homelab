apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ldap-secret
  namespace: ldap
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  data:
    - secretKey: DIR_MANAGER_PASSWORD
      remoteRef:
        key: /ldap/dir-manager
        property: password
