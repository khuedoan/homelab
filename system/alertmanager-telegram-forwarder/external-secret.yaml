apiVersion: kubernetes-client.io/v1
kind: ExternalSecret
metadata:
  name: kubernetes-internal-ca-key-pair
spec:
  backendType: vault
  data:
    - key: secret/data/alertmanager-telegram-forwarder/telegram
      name: token
      property: token
