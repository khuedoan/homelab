apiVersion: snapscheduler.backube/v1
kind: SnapshotSchedule
metadata:
  name: jackett-zfs-backups
spec:
  retention:
    maxCount: 20
  schedule: "0 3 * * *"
  claimSelector:
    matchLabels:
      backup: jackett-zfs
  snapshotTemplate:
    snapshotClassName: zfs-local-dataset
