apiVersion: snapscheduler.backube/v1
kind: SnapshotSchedule
metadata:
  name: nextcloud-zfs-backups
spec:
  retention:
    maxCount: 20
  schedule: "0 3 * * *"
  claimSelector:
    matchLabels:
      backup: nextcloud-zfs
  snapshotTemplate:
    snapshotClassName: zfs-local-dataset
