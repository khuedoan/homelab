grocy:
  env:
    TZ: Europe/Zurich
  persistence:
   config:
     enabled: true
     mountPath: "/config"
     size: 10Gi
  ingress:
    main:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        hajimari.io/appName: Grocy
        hajimari.io/icon: home
      hosts:
        - host: &host grocy.tribz.cloud
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: grocy-tls-certificate
          hosts:
            - *host