apiVersion: v1
kind: Endpoints
metadata:
  name: grigri-ports
subsets:
- addresses:
  - ip: 192.168.192.2
  ports:
{{- range .Values.ports }}
  - name: {{ .name }}
    port: {{ .port }}
    protocol: TCP
{{- end }}
