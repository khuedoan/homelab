apiVersion: v1
kind: Service
metadata:
  name: grigri-ports
spec:
  ports:
{{- range .Values.ports }}
  - name: {{ .name }}
    port: {{ .port }}
    protocol: TCP
    targetPort: {{ .port }}
{{- end }}
  clusterIP: None
  type: ClusterIP
