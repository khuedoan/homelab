apiVersion: v1
kind: Endpoints
metadata:
  name: grigri-ports-udp
subsets:
- addresses:
  - ip: 192.168.192.2
  ports:
{{- range .Values.ports.udp }}
  - name: {{ .name }}
    port: {{ .port }}
    protocol: UDP
{{- end }}
