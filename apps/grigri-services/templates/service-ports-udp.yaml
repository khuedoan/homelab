apiVersion: v1
kind: Service
metadata:
  name: grigri-ports-udp
spec:
  ports:
{{- range .Values.ports.udp }}
  - name: {{ .name }}
    port: {{ .port }}
    protocol: UDP
    targetPort: {{ .port }}
{{- end }}
  clusterIP: None
  type: ClusterIP
