qbittorrent:
  controller:
    replicas: 1
  env:
    TZ: Europe/Zurich
  persistence:
    config:
      enabled: true
      mountPath: /config
      accessMode: ReadWriteOnce
      size: 1Gi
    downloads:
      enabled: true
      mountPath: /data/torrents
      existingClaim: mediastack-data
      subPath: torrents/
  ingress:
    main:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        hajimari.io/appName: qBittorrent
        hajimari.io/icon: pirate
        hajimari.io/group: arr
      hosts:
        - host: &host qbittorrent.danger250.com
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: qbittorrent-tls-certificate
          hosts:
            - *host
radarr:
  controller:
    replicas: 1
  env:
    TZ: Europe/Zurich
  image:
    repository: lscr.io/linuxserver/radarr
    tag: develop
  persistence:
    config:
      enabled: true
    data:
      enabled: true
      mountPath: /data
      existingClaim: mediastack-data
  ingress:
    main:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        hajimari.io/appName: radarr
        hajimari.io/icon: pirate
        hajimari.io/group: arr
      hosts:
        - host: &host radarr.danger250.com
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: radarr-tls-certificate
          hosts:
            - *host
kube-plex:
  # use this when expanding config disk, this frees the PVC from the pod
  controller:
    replicas: 1
  timezone: Europe/Zurich
  claimToken: claim-CJkYTtiRhSuVgqFaU3rm
  persistence:
    transcode:
      enabled: true
      size: 2Gi
    config:
      enabled: true
      size: 5Gi
    data:
      enabled: true
      mountPath: /data/media
      claimName: mediastack-data
      subPath: media/
  ingress:
    enabled: false
jellyfin:
  # use this when expanding config disk, this frees the PVC from the pod
  controller:
    replicas: 1
  env:
    TZ: Europe/Zurich
  image:
    repository: lscr.io/linuxserver/jellyfin
    tag: latest
  persistence:
    config:
      enabled: true
      size: 10Gi
    data:
      enabled: true
      mountPath: /data/media
      existingClaim: mediastack-data
      subPath: media/
  ingress:
    main:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        external-dns.alpha.kubernetes.io/target: "homelab-tunnel.danger250.com"
        external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
        hajimari.io/appName: Jellyfin
        hajimari.io/icon: multimedia
      hosts:
        - host: &host jellyfin.danger250.com
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: jellyfin-tls-certificate
          hosts:
            - *host
    local:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        hajimari.io/appName: Jellyfin-Local
        hajimari.io/icon: multimedia
      hosts:
        - host: &host jellyfin-local.danger250.com
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: jellyfin-local-tls-certificate
          hosts:
            - *host
  resources:
    limits:
      gpu.intel.com/i915: 1
lidarr:
  controller:
    replicas: 1
  env:
    TZ: Europe/Zurich
  persistence:
    config:
      enabled: true
    data:
      enabled: true
      mountPath: /data
      existingClaim: mediastack-data
  image:
    repository: lscr.io/linuxserver/lidarr
    tag: develop
  ingress:
    main:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        hajimari.io/appName: lidarr
        hajimari.io/icon: pirate
        hajimari.io/group: arr
      hosts:
        - host: &host lidarr.danger250.com
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: lidarr-tls-certificate
          hosts:
            - *host
sonarr:
  controller:
    replicas: 1
  env:
    TZ: Europe/Zurich
  persistence:
    config:
      enabled: true
    data:
      enabled: true
      mountPath: /data
      existingClaim: mediastack-data
  image:
    repository: lscr.io/linuxserver/sonarr
    tag: develop
  ingress:
    main:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        hajimari.io/appName: Sonarr
        hajimari.io/icon: pirate
        hajimari.io/group: arr
      hosts:
        - host: &host sonarr.danger250.com
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: sonarr-tls-certificate
          hosts:
            - *host
readarr:
  controller:
    replicas: 1
  env:
    TZ: Europe/Zurich
  persistence:
    config:
      enabled: true
      size: 2Gi
    data:
      enabled: true
      mountPath: /data
      existingClaim: mediastack-data
  image:
    repository: lscr.io/linuxserver/readarr
    tag: develop
  ingress:
    main:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        hajimari.io/appName: Readarr
        hajimari.io/icon: pirate
        hajimari.io/group: arr
      hosts:
        - host: &host readarr.danger250.com
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: readarr-tls-certificate
          hosts:
            - *host
prowlarr:
  controller:
    replicas: 1
  env:
    TZ: Europe/Zurich
  persistence:
    config:
      enabled: true
      size: 2Gi
  image:
    repository: lscr.io/linuxserver/prowlarr
    tag: develop
  ingress:
    main:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        hajimari.io/appName: prowlarr
        hajimari.io/icon: sync
        hajimari.io/group: arr
      hosts:
        - host: &host prowlarr.danger250.com
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: prowlarr-tls-certificate
          hosts:
            - *host
bazarr:
  controller:
    replicas: 1
  podSecurityContext:
    runAsUser: 911
    runAsGroup: 911
    fsGroup: 911
    supplementalGroups: []
    fsGroupChangePolicy: OnRootMismatch
  securityContext:
    readOnlyRootFilesystem: false
    allowPrivilegeEscalation: true
    privileged: true
  env:
    TZ: Europe/Zurich
  persistence:
    config:
      enabled: true
      size: 1Gi
    media:
      enabled: true
      mountPath: /data
      existingClaim: mediastack-data
  ingress:
    main:
      enabled: true
      ingressClassName: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        hajimari.io/appName: bazarr
        hajimari.io/icon: pirate
        hajimari.io/group: arr
      hosts:
        - host: &host bazarr.danger250.com
          paths:
            - path: /
              pathType: Prefix
      tls:
        - secretName: bazarr-tls-certificate
          hosts:
            - *host