apiVersion: v1
kind: Service
metadata:
  name: {{ include "tm-evosc.fullname" . }}
  labels:
    {{- include "tm-evosc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tm-evosc.selectorLabels" . | nindent 4 }}
