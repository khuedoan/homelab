apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tm-evosc.fullname" . }}-test-connection"
  labels:
    {{- include "tm-evosc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tm-evosc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
