apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tm-evosc-chart.fullname" . }}-test-connection"
  labels:
    {{- include "tm-evosc-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tm-evosc-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
