elementweb:
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
      external-dns.alpha.kubernetes.io/target: "homelab-tunnel.falconpunch.org"
      external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
    hosts:
      - host: &frontend_host chat.falconpunch.org
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: element-tls-certificate
        hosts:
          - *frontend_host
  config:
    default:
      base_url: https://matrix.falconpunch.org
      server_name: falconpunch.org

dendrite:
  dendrite_config:
    global:
      server_name: matrix.falconpunch.org
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hostName: matrix.falconpunch.org
    tls:
      - hosts:
          - matrix.falconpunch.org
        secretName: matrix-tls-certificate
  postgresql:
    enabled: true
