apiVersion: snapscheduler.backube/v1
kind: SnapshotSchedule
metadata:
  name: wallabag-zfs-backups
spec:
  retention:
    maxCount: 20
  schedule: "0 3 * * *"
  claimSelector:
    matchLabels:
      backup: wallabag-zfs
  snapshotTemplate:
    snapshotClassName: zfs-local-dataset
