---
# TODO: apply k8s labels
node_labels:
  storage-node: "true"

prepare_additional_disks:
 - dev: /dev/sda
   # path: "/var/lib/longhorn/{{ ansible_devices['sda'].model }}"
   path: /var/lib/longhorn
